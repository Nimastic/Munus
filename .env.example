# Root environment variables (if needed for monorepo scripts)
# Most configuration is in package-specific .env files:
#   - apps/miniapp/.env.local (or .env.example)
#   - packages/agent/.env (or .env.example)
#   - packages/contracts/.env (or .env.example)

# Optional: Global RPC URLs
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
MAINNET_RPC_URL=https://eth.llamarpc.com
